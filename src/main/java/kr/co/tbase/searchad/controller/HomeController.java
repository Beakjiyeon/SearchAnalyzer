package kr.co.tbase.searchad.controller;

import kr.co.tbase.searchad.service.HomeService;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;

@Slf4j
@Controller
public class HomeController {
/*
    @Autowired
    HomeService homeService;

    @GetMapping(value = { "/join"})
    public String join(Model models) {
        log.debug("call join.html");
        // models.addAttribute("date", homeService.getStringCurrentDate());
        return "join.html";
    }

    @GetMapping(value = { "/login"})
    public String login(Model models) {
        log.debug("call login.html");
        // models.addAttribute("date", homeService.getStringCurrentDate());
        return "login.html";
    }

    @GetMapping(value = { "/admin/users"})
    public String adminUsers(Model models) {
        log.debug("call login.html");
        // models.addAttribute("date", homeService.getStringCurrentDate());
        return "admin.html";
    }

    @GetMapping(value = { "/search"})
    public String search(Model models) {
        log.debug("call login.html");
        // models.addAttribute("date", homeService.getStringCurrentDate());
        return "search.html";
    }

    @GetMapping(value = { "/host"})
    public String host(Model models) {
        log.debug("call login.html");
        // models.addAttribute("date", homeService.getStringCurrentDate());
        return "host.html";
    }

    @GetMapping(value = { "/relatedwords"})
    public String getRelatedWords(Model models) {
        log.debug("call login.html");
        // models.addAttribute("date", homeService.getStringCurrentDate());
        return "relatedWords.html";
    }
*/
}
